module.exports = {
    // enabled logging for development
    logging: true,
    // seed the db
    seed: false,
    // set db to localhost/dbname
    db: {
        url: 'mongodb://localhost/merndb'
    }
};